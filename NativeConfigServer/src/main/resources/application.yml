server:
  port: 8762
spring:
  application:
    name: configServer
  profiles:
    active: native  #读取本地配置
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared